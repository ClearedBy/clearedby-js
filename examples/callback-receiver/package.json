{
  "name": "@clearedby/example-callback-receiver",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fire-and-forget: gate an action with a callbackUrl, then verify + act on the signed verdict webhook.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clearedby/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2"
  }
}
