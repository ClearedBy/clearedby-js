{
  "name": "@clearedby/example-shopify-refund-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dogfood demo: a Shopify merchant's AI agent issues a refund through ClearedBy — propose → gate → human approves/sends-back → agent revises → execute → prove.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clearedby/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2"
  }
}
