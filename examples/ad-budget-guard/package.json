{
  "name": "@clearedby/example-ad-budget-guard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "An ads agent gating ad.launch — check() dry-run, then gate + poll for the decision.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clearedby/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2"
  }
}
