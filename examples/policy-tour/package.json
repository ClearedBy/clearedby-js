{
  "name": "@clearedby/example-policy-tour",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dry-run check() across all example policies — prints the verdict matrix, records nothing.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clearedby/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2"
  }
}
